import { Test, TestingModule } from '@nestjs/testing';
import { PipelineGenericService } from './pipeline-generic.service';

describe('PipelineGenericService', () => {
//   let service: PipelineGenericService;
//
//   beforeEach(async () => {
//     const module: TestingModule = await Test.createTestingModule({
//       providers: [PipelineGenericService],
//     }).compile();
//
//     service = module.get<PipelineGenericService>(PipelineGenericService);
//   });

  it('should be defined', () => {
  //   expect(service).toBeDefined();
  });
});
